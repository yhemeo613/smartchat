services:
  smartchat:
    image: smartchat:latest
    container_name: smartchat
    ports:
      - "8889:8889"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=8889
    restart: always
